# Parser reference

`GmlParseAPI.hx` is GMEdit's parser for the `api.gml` annotation format (`github.com/YellowAfterlife/GMEdit`, `src/parsers/GmlParseAPI.hx`, MIT). `tools/generate-api.ts` ports the relevant regexes from it. Reference only; not compiled.
